This is a horizontal menu where some items trigger drop down options. 

This needs to work in Firefox 2.0 
This needs to work on touch screens,
  this only means that the drop down options should show not just on hover of the parent but also 
  when it is clicked
